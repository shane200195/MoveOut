# MoveOut

# Usage

To build the frontend: 
`
yarn install
`

To run the frontend:
```
npm start
```

To build the backend 
```
pip install -r requirements.txt
```

To run the backend:
```
python main.py
```
